---
name: xiazhouqi-clear
description: "Build or refine calm, information-first, mobile-reliable web UI in Xiazhouqi Clear｜下周七·清透. Use Apple-like restraint as the skeleton without copying Apple; use grouping, typography, three-level shadow, and low-saturation semantic color to organize dense real-world information. Trigger on xiazhouqi-clear, Xiazhouqi Clear, 下周七·清透, 清透风格, 苹果感但不要照抄苹果, 信息多但要耐看."
metadata:
  version: "1.2.0"
---

# Xiazhouqi Clear｜下周七·清透

**Repository:** `xiazhouqi-clear-skill`  
**Version:** `1.2.0`
**Primary use:** 信息密集型 HTML、私人档案、账号管理、银行卡/财务、下载入口、工具页、管理页  
**Core character:** 清透、克制、耐看、信息优先、层级明确、移动端可靠

> **不全抄苹果，但保留苹果的克制；不堆满颜色，但允许颜色承担信息层级**

执行层原则：

> **结构负责秩序，字重负责重点，阴影负责层级，颜色负责分类**

Clear 不是纯黑白灰，也不是 Apple 官网复刻。它面向大量真实信息和长期使用场景：以 Apple 式低噪音骨架为基础，再通过低饱和语义色、分组、字重、阴影和留白建立自己的信息层级。

---

# 1. 适用范围

优先用于：

- 邮箱档案
- Apple ID / 外区账号档案
- 银行卡档案
- 固定支出 / 财务规划
- 私人档案馆总入口
- 软件 / 工具下载页
- 账号管理页
- 数据清单
- 登录信息整理页
- 个人后台式信息页面
- 需要长期维护的单文件 HTML

不优先用于：

- 强情绪表达页面
- 生活随笔 / 日记
- 强叙事纪念页
- 需要明显温度、奶杏、柔和文学感的页面

以上类型优先考虑 `Xiazhouqi Warm｜下周七·暖杏`。

---

# 2. Clear 的核心设计哲学

## 2.1 内容优先

页面首先是使用工具，其次才是视觉作品。任何调整都必须服务于：

1. 更快找到信息
2. 更快理解分类
3. 更快识别重要程度
4. 更舒服地长期阅读
5. 手机端稳定使用

禁止为了“更像 Apple”牺牲信息辨识度。

## 2.2 Clear ≠ 纯黑白灰

基础视觉以冷白、浅灰、轻透明白、极淡边框、低对比背景、克制圆角、轻阴影、大量留白、黑 / 深灰正文为主。

信息复杂时允许使用**低饱和语义色**。

> 白色承载内容，颜色负责导航

## 2.3 先分组，再美化

先判断平台、地区、用途、优先级、主 / 备用 / 历史，再决定组件、颜色和视觉权重。

同一地区多个 Apple ID 应聚合；同一邮箱平台应放在同一平台区；多张核心定存应形成核心资产组；低频备用应统一弱化。

禁止把所有内容机械做成同尺寸、同阴影、同颜色的一长串卡片。

---

# 3. 信息层级系统

| 手段 | 负责什么 |
|---|---|
| 结构 / 分组 | 信息之间的关系 |
| 字重 | 信息的重要程度 |
| 阴影 | 卡片的视觉层级 |
| 颜色 | 信息所属类别 |

四者不要互相替代。

## 3.1 字重

- 标题 / 核心账号 / 主卡：`700–760`
- 关键金额：`760–800`
- 普通账号：`600–700`
- 正文：`400–500`
- 历史 / 备用：降低对比，不要缩小到难读

> 重要内容变稳，次要内容变轻，不靠夸张字号制造层级

## 3.2 三档阴影

### Level 1 — Core

主定存、第二定存、第三定存、主邮箱、核心总控等真正需要优先注意的内容。

```css
box-shadow:
  0 1px 2px rgba(15,23,42,.035),
  0 14px 34px rgba(15,23,42,.075);
```

### Level 2 — Normal

普通一类卡、普通邮箱、日常消费账户、正常内容卡。

```css
box-shadow: 0 4px 14px rgba(15,23,42,.035);
```

### Level 3 — Quiet

二类卡、低频备用、历史账号、冷备份、不常用内容。

```css
box-shadow: none;
```

硬规则：

- 不要所有卡片阴影相同
- 不要为了立体给每张卡大阴影
- 阴影表达重要程度，不表达类别

详见 `references/hierarchy-and-color.md`。

---

# 4. 颜色系统

## 4.1 基础中性色

```css
:root {
  --ground-a: #fbfbfd;
  --ground-b: #f3f4f7;
  --surface: #ffffff;
  --section-bg: rgba(255,255,255,.93);
  --line: rgba(60,60,67,.08);
  --line-strong: rgba(60,60,67,.13);
  --text-main: #1d1d1f;
  --text-body: #4b4b50;
  --text-secondary: #6e6e73;
  --text-faint: #8e8e93;
}
```

不得偏暖到奶杏风，也不得变成强烈冷蓝科技风。

## 4.2 语义色

颜色必须先回答：**它代表什么？**

无法解释分类含义，就不要加。

推荐颜色载体：

- 顶部 2–3px 细线
- 小圆点
- 小标签
- 极淡边框
- 极淡 wash 背景
- 进度条
- 状态提示

卡片主体继续以白色为主。

## 4.3 中性主导与防止“整页同色化”（硬规则）

Clear 的语义色是定位信号，不是整页主题漆。即使主色是蓝色，也不能让 body、Header、Section、Card、Tag、按钮、复选框和进度条同时大面积使用同一蓝色。

硬规则：

- body、Header、Section、Card 默认使用冷白 / 浅灰 / 透明白
- 每个视觉层级只选 `1–2` 种颜色载体，例如“顶部细线 + 小圆点”或“极淡 edge + 进度条”
- 父容器已有彩色顶部线或 edge 时，内部卡片保持白色；内部 callout（提示块）使用 wash 时，父容器保持中性
- 标题和正文默认使用深灰，不要把所有标题都染成主色
- 三个及以上并列类别应使用不同的低饱和语义色，或让次要类别保持中性；不得全部套成蓝色卡片
- 同一页面允许蓝、青灰绿、灰紫、暖沙金等共同存在，但每种颜色必须对应稳定分类
- 远看或眯眼时，如果页面首先被识别成“一整张蓝色页面”，即判定失败

可用的中性 / 语义比例仍是 `70–85% / 15–30%`，但这里的 15–30% 指视觉线索，不是染色面积。

## 4.4 已确认样板优先级（硬规则）

当用户指定已经做好的页面作为样板时，已确认页面优先于本 Skill 中更抽象的文字规则。当前基准是总档案馆中的最新版“邮箱”和“消费支出规划”：

- 邮箱：中性白灰骨架，平台色只用于顶部线、小圆点、标签、极淡 edge / wash；重复账号可用统一列表面板与分隔线
- 消费规划：中性白灰骨架，不同资金类别分别使用灰蓝紫、暖沙棕、青灰绿、香槟金；总额与提醒保持中性
- 应提取最终视觉规律，不复制旧文件里层层叠加的历史补丁
- 样板内容可能包含私人数据；只学习结构、比例和颜色逻辑，不把真实内容写进通用 Skill 或示例

详见 `references/canonical-visual-patterns.md`。

## 4.5 强度

- 实色：只用于极小圆点 / 标识
- wash 背景：约 `0.04–0.08` alpha
- 边框：约 `0.12–0.18` alpha

```css
--accent: #2f7de1;
--wash: rgba(47,125,225,.06);
--edge: rgba(47,125,225,.14);
```

---

# 5. 不同页面的颜色逻辑

## 5.1 邮箱：按平台分色

- Gmail → 淡砖红 / 灰红
- Outlook → 雾霾蓝
- QQ / Foxmail → 青绿色
- Apple / iCloud → 银灰 / 灰紫
- 阿里云 → 天青蓝
- 新浪 → 淡暖橙

平台内卡片仍以白色为主。主邮箱靠字重和阴影突出；历史邮箱退灰。不要 Gmail 1 / Gmail 2 / Gmail 3 各用一种颜色。

## 5.2 Apple ID：按地区分色并聚合同地区账号

- 美国 → 灰蓝
- Türkiye → 灰陶土
- 日本 / 韩国 → 灰紫 / 冷蓝紫
- 印度 / 新加坡 → 青灰
- 尼日利亚 / 菲律宾 → muted olive（低饱和橄榄绿）
- 中国大陆 / 港台 → 更中性

同地区多个账号优先聚合到同一地区卡片。

## 5.3 银行卡：按资金用途分色

**禁止按银行品牌上色。**

- 核心定存 → 香槟金 / 暖灰金 + Level 1
- 日常资金流转 → 雾蓝 / 蓝青 + Level 2
- 低频备用 → 中性灰 + Level 3
- 小额虚拟 → 极淡紫灰 / 青灰

## 5.4 消费规划：按资金类别分色

- 虚拟消费 → 灰蓝紫
- 住房消费 → 暖沙棕
- 账户用途分配 → 青灰绿
- 每月定存 → 香槟金
- 固定总额 / 提醒 → 中性深灰

详见 `references/content-patterns.md`。

---

# 6. 页面骨架

```css
body {
  min-height: 100vh;
  padding: 14px 10px 36px;
  background: linear-gradient(180deg,#fbfbfd,#f3f4f7);
  color: #1d1d1f;
  font-family: -apple-system,BlinkMacSystemFont,"SF Pro Text","SF Pro Display",system-ui,"PingFang SC","Microsoft YaHei",sans-serif;
  -webkit-font-smoothing: antialiased;
}

.page {
  max-width: 1080px;
  margin: 0 auto;
}

@media (min-width:720px) {
  body { padding: 14px 22px 56px; }
}
```

---

# 7. 圆角规范

```css
:root {
  --r-header: 24px;
  --r-section: 23px;
  --r-core: 20px;
  --r-normal: 18px;
  --r-pill: 999px;
}
```

Header 最大，Section 次之，Core card 稍大，Normal card 略小，Tag / Pill 使用胶囊。不要所有组件都 30px+ 巨型圆角。

---

# 8. Header 规范

Header 只负责告诉用户“这是什么”，不要占据半屏。

- 手机标题约 28–32px
- 桌面约 36–40px
- 字重约 740–780
- 不使用巨大宣传 Hero
- 不增加无价值大面积装饰光斑
- 不增加与档案功能无关的宣传文案

---

# 9. 文案规则（硬规则）

## 9.1 标题下默认不主动添加解释性副标题

优先结构：`标签 / 标题 → 数据`。

用户没有要求时，不要为了让页面“像设计稿”而添加说明性句子。

## 9.2 必须有副标题时

短、克制、与功能有关、不喧宾夺主、不写成散文。

## 9.3 短文案默认不加句号

标题、标签、副标题、页尾短句、顶部短说明、导航、一句话提示，结尾默认不加 `。`。

## 9.4 Clear 与 Warm 的语言边界

Clear 简短、稳定、功能性强、轻微审美感；Warm 可以更文艺、更有生活感。不要把 Warm 的语言习惯搬进 Clear。

详见 `references/copy-and-content.md`。

---

# 10. Section 规范

Section 是大分组，不是装饰容器。

```css
.section {
  padding: 16px 13px;
  border-radius: var(--r-section);
  background: rgba(255,255,255,.93);
  border: 1px solid rgba(60,60,67,.08);
  box-shadow: 0 1px 2px rgba(15,23,42,.03),0 10px 30px rgba(15,23,42,.045);
}
```

可以使用语义色顶部细线、淡色 tag、淡色 edge，但不要整块明显染色。

---

# 11. Card 规范

```css
.card {
  border-radius: var(--r-normal);
  border: 1px solid rgba(60,60,67,.08);
  background: linear-gradient(180deg,#fff,#fcfcfd);
  padding: 14px 13px;
}
```

Core 卡使用更明显但仍克制的阴影；historical / low-frequency（历史 / 低频）卡降低对比并可取消阴影。

---

# 12. 标签规范

标签只表达一个维度：用途、类型、地区、账号角色或卡等级。

推荐 10–11px、4–8px padding、pill、低饱和 wash、细边框。

避免：标签比标题更显眼、单卡塞太多彩色标签、所有标签不同颜色。

---

# 13. 复制按钮规范

复制属于辅助工具，不是主操作。

视觉：圆形、小尺寸、浅灰、低透明度、轻边框，不使用高饱和蓝。

```css
.copy-btn {
  width: 22px;
  height: 22px;
  border-radius: 999px;
  border: 1px solid rgba(60,60,67,.13);
  background: #f6f6f8;
  color: #6e6e73;
  opacity: .62;
}
```

视觉可以小，但点击必须稳定：

- 装饰伪元素 `pointer-events:none`
- 按钮层级高于装饰层
- 必要时通过 wrapper 扩大真实触控范围
- 卡片点击事件不得抢走复制按钮事件

---

# 14. 导航规范

大量分区可用顶部 pill nav（胶囊导航）。导航要小、轻，不默认 sticky（吸顶）到遮挡正文。

手机端优先保持自然流；短分类数量不多时尽量完整一排，必要时再横向滚动或使用紧凑 grid（网格）。

导航可带语义色小圆点，但本身不承担主视觉。

---

# 15. 移动端优先（硬规则）

默认以 iPhone 真实使用为第一优先级。

必须检查：390px、360–380px、长邮箱、长卡号、长标签、多按钮、中英混排。

## 15.1 禁止横向溢出

```css
* { box-sizing: border-box; }
html,body { max-width:100%; overflow-x:hidden; }
.breakable { min-width:0; overflow-wrap:anywhere; }
```

Grid 使用 `minmax(0,1fr)`，不要让固定宽度撑破页面。

## 15.2 输入框防 iPhone 自动放大

```css
@supports (-webkit-touch-callout:none) {
  input,textarea,select,button { font-size:16px; }
}
```

详见 `references/mobile-and-compatibility.md`。

---

# 16. JavaScript 使用原则（硬规则）

关键进入、栏目切换、正文显示、主要导航，在 iPhone 本地 HTML 预览中应有可靠降级方案。

JavaScript（脚本）优先用于增强：复制、Toast（轻提示）、小动画、非必要交互。

> JS 失败，页面仍然能读、能进、能找内容

轻量访问门槛可以使用 `:target`、checkbox + `:checked`、兼容环境下的 `:has()` 等机制，并用 JS 增强。

注意：前端单文件“密码”只是界面访问门槛，不是真正数据加密。不要声称它能保护 HTML 源码中的敏感数据。

---

# 17. 禁止 iframe / srcdoc 承载核心档案内容（硬规则）

在 iPhone 本地 HTML 预览中，iframe、srcdoc、动态注入整页 HTML 可能出现外框正常但正文空白。

> **核心正文必须直接内嵌在当前 DOM 中**

总档案馆可通过 radio / checkbox / `:checked` / `:target` / CSS selector 切换模块，不要通过 iframe 加载核心子页面正文。

---

# 18. 总档案馆 / 聚合页规则

总入口负责统一导航、页面骨架、返回目录和 Clear 气质，但**不重新发明内部模块设计**。

内部模块继承各自已确认逻辑：

- 邮箱 → 平台色
- 银行卡 → 用途色
- Apple ID → 地区色
- 消费规划 → 资金类别色

禁止独立页已经是最新版，整合进档案馆后却退回旧设计 / 旧数据。

---

# 19. 数据与内容同步规则（硬规则）

如果用户只说“改风格”：

- 保留原始信息
- 不擅自增删账号
- 不擅自改金额
- 不擅自改用途
- 不擅自改卡号

如果用户明确要求“按最新标准同步”，内容优先级：

1. 当前对话中用户最新明确确认的信息
2. 当前对话中已经做好的最新独立版本
3. 最新上传且内容明确的文件
4. 总档案馆中嵌入的旧副本
5. 更早历史文件

来源冲突时优先最新明确确认；无法确定时不要静默猜测。

---

# 20. 银行卡用途逻辑示例

示例层级：

- 主定存 / 第二定存 / 第三定存 → Level 1
- 后续常用一类卡 → Level 2
- 二类卡 / 低频备用 → Level 3
- 小额虚拟 → Quiet 中做轻微类别区分

具体银行和账户数据属于用户档案内容，不应被当成所有 Clear 页面通用数据。

---

# 21. 信息组件选择

不要为了统一强迫所有数据都变成 Card（卡片）。可根据内容使用 Card、List row（列表行）、Group panel（分组面板）、Mini row（迷你行）、Pill、Section、Summary block（汇总块）、Progress bar（进度条）。

统一的是字体、圆角、边框、留白、中性色、色彩逻辑和阴影逻辑，不是“所有东西长得一样”。

---

# 22. 响应式布局建议

手机：默认单列、卡片 100%、信息自然换行、核心按钮保持可点击、避免标题与 tag 横向互挤。

平板 / 桌面：根据内容密度决定 2 列卡片、3 列 mini list 或主次双栏。不要为了填满屏幕强行多列。

---

# 23. 页面密度

Clear 的留白不是空旷。

- section 间距约 24–30px
- card gap 约 10–14px
- card padding 约 13–18px
- 信息列表行高约 1.65–1.8

手机端不应出现大块无意义空白。

---

# 24. 图标 / Emoji 规范

优先小圆点、简洁线性 SVG、极简符号、文字标签。

Emoji 仅在原内容确实需要或用户明确喜欢时使用。避免大量彩色 Emoji 当栏目主视觉，避免图标比信息更抢眼。

---

# 25. 动效规则

推荐 120–200ms、opacity、scale 0.97–1、translateY 1–4px。

禁止大幅漂浮、发光扫描、强烈 3D 倾斜、长时间循环动画、干扰阅读的背景运动。

档案页不是展示 Demo。

---

# 26. HTML 代码维护规则

应该：

- 整理为一套最终 CSS
- 合并重复 media query
- 删除失效旧样式
- 保留清楚注释
- class 语义明确
- 保留数据完整性

不应该：

- 文件末尾无限追加 v1/v2/v3/v4 修补 CSS
- 同 selector 被十几次 `!important` 覆盖
- 修手机问题时破坏桌面
- 没必要时引入框架

旧文件历史补丁过多时，优先干净重构。

---

# 27. 单文件优先

私人档案页优先单 `.html`、CSS 内联、SVG 内联、无 CDN（内容分发网络）、无网络字体、无外部 JS。

原因：本地可打开、GitHub Pages 可直接部署、iPhone 文件预览更稳定、易归档、减少第三方失效影响。

---

# 28. 可访问性与可用性

至少保证：

- 正文对比度足够
- 重要文字不只靠颜色区分
- 按钮有 `aria-label`
- 输入框有清楚 label / placeholder
- 点击区稳定
- 键盘 Enter 在 JS 可用环境可触发
- 焦点状态可辨识但不花哨

颜色只是辅助信息，不是唯一载体。

---

# 29. 不要做的事

默认禁止：

- 强奶杏背景
- 暖橙大面积毛玻璃
- 过度渐变
- 高饱和多色卡片
- 全页背景、所有卡片、标签和控件同时套同一蓝色
- 父容器与所有子容器重复使用同一 accent wash（强调淡底）
- 将“蓝色风格”理解成“蓝色面积越多越统一”
- 大段文艺说明
- 无意义 slogan
- 每个模块不同视觉语言
- 巨型按钮
- 大量 Emoji
- 重阴影遍地
- 为了像 Apple 牺牲信息层次
- iframe 承载核心内容
- 核心功能仅依赖 JS
- 手机横向溢出
- 无意义底部说明文字

---

# 30. Clear 与 Apple 感的关系

可以借鉴 Apple 的留白、字体层级、克制标题、轻边框、圆角、低噪音、清晰主次、移动端适配。

不要机械复制 Apple 的过度单色、展示型大 Hero、适合少量内容的超大留白、只适合官网不适合信息库的布局。

因为 Clear 面对的是**大量个人信息**。

---

# 31. Clear 的最终视觉平衡

目标不是纯 Apple、纯极简、纯彩色或纯玻璃，而是：

> **70–85% 中性骨架 + 15–30% 语义色层级**

颜色越多，饱和度越低；信息越重要，越优先用结构 / 字重 / 阴影表达，而不是增加鲜艳度。

---

# 32. 执行流程

当用户说“按 xiazhouqi-clear-skill 改”时：

1. **读取源文件**：确认结构、数据、功能、密码机制、手机适配、当前交互
2. **判断是否只改风格**：未要求同步数据则不改内容；要求最新标准则按最新确认版本同步
3. **重新分组信息**：识别平台、地区、用途、重要程度、历史 / 当前
4. **定义语义色**：每个颜色都必须有分类含义
5. **分配颜色载体**：每层只保留 1–2 个载体，先检查是否出现整页同色化
6. **建立视觉层级**：组合字重、阴影、颜色、留白、组件类型
7. **移动端优先改造**：检查溢出、长字符串、按钮点击、输入框放大、密码入口、本地预览
8. **核心功能降级**：确保 JS 不运行时核心正文仍可访问，不用 iframe / srcdoc 承载正文
9. **文案清理**：删多余说明、删过度文艺句、短句去句号
10. **代码清理**：删旧冲突样式，不堆 patch，输出最终干净单文件
11. **最终检查**：数据未丢、复制按钮未遮挡、手机无横向滚动、同类颜色一致、层级正确、不过度彩色

---

# 33. 验收清单

完成前逐项确认：

- [ ] 第一眼干净、克制
- [ ] 不像纯黑白模板
- [ ] 不像彩色 Dashboard
- [ ] 颜色都有明确语义
- [ ] 同类信息使用同一颜色体系
- [ ] Core / Normal / Quiet 层级正确
- [ ] 主要信息字重清楚
- [ ] 旧 / 备用信息有弱化
- [ ] 卡片主体仍以白色为主
- [ ] body、Header、Section 没有被同一强调色大面积染色
- [ ] 每个视觉层级只使用 1–2 种颜色载体
- [ ] 父容器与子卡没有重复叠加同一 wash
- [ ] 三个以上类别没有全部套成同一种蓝色
- [ ] 390px 无横向溢出
- [ ] 360–380px 仍可使用
- [ ] 长邮箱 / 卡号可换行
- [ ] iPhone 输入框不自动放大
- [ ] 核心内容不依赖 iframe
- [ ] 核心进入 / 切换不只依赖 JS
- [ ] 复制按钮小而淡，但稳定点击
- [ ] 没有无意义说明文案
- [ ] 短标题 / 标签 / 副标题末尾无句号
- [ ] 要求同步时使用用户最新确认内容
- [ ] HTML 没有堆叠大量旧 CSS patch

详见 `references/maintenance-and-checklist.md`。

---

# 34. v1.2.0 新增重点

1. 以最新版邮箱 / 消费支出规划作为 canonical samples（最终确认样板）
2. 明确中性白灰骨架高于页面主色
3. 禁止 body、Section、Card、Tag、控件同时套同一蓝色
4. 每个视觉层级只保留 1–2 种颜色载体
5. 增加父子容器避免重复 wash 的规则
6. 增加三类以上内容的多语义色 / 中性退后规则
7. 增加任务清单、年度计划、历史记录页模式
8. 保留 v1.1.0 的语义色、三档层级、内容同步和移动端可靠性规则

---

# 35. 最终记忆口诀

1. **Clear 不是纯黑白灰**
2. **白色承载内容，颜色负责导航**
3. **先分组，再美化**
4. **字重管重点，阴影管层级，颜色管分类**
5. **苹果感是骨架，不是限制**
6. **短文案克制，默认不加句号**
7. **手机能稳定用，比桌面 Demo 好看更重要**
8. **不全抄苹果，但保留苹果的克制；不堆满颜色，但允许颜色承担信息层级**

---

## Reference loading

按任务需要读取：

- `references/hierarchy-and-color.md` — 色彩、阴影、层级细则
- `references/canonical-visual-patterns.md` — 邮箱 / 消费规划最终样板与任务页映射
- `references/content-patterns.md` — 邮箱 / Apple ID / 银行卡 / 消费规划模式
- `references/mobile-and-compatibility.md` — iPhone、本地 HTML、密码入口、复制交互
- `references/copy-and-content.md` — 文案和内容同步
- `references/maintenance-and-checklist.md` — 代码清理和最终验收
- `tokens.css` — 可复用 token（设计变量）
- `reference.html` — 最小参考实现

**Xiazhouqi Clear｜下周七·清透 v1.2.0**
